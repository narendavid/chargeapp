// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyBW9t9IAATb26SjPguS0QevL1Ad670kHSw",
    authDomain: "chargeapp-2783a.firebaseapp.com",
    databaseURL: "https://chargeapp-2783a.firebaseio.com",
    projectId: "chargeapp-2783a",
    storageBucket: "chargeapp-2783a.appspot.com",
    messagingSenderId: "820240819736",
    appId: "1:820240819736:web:8b32e192f0ad3d4501856d"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
